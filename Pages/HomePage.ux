<Page ux:Class="HomePage">
	<JavaScript File="HomePage.js" />
	<ScrollView>
		<StackPanel>
			<Each Items="{hikes}">
				<Button Text="{name}" Clicked="{choseHike}"></Button>
			</Each>
		</StackPanel>
	</ScrollView>
</Page>
